#!/bin/bash
# if a corresponding entry in fstab exists (and the current
# user is allowed to mount it), we can also use a short version
# using only the mount point as a parameter:
mount /media/hapebe/austausch
